#!/usr/bin/make -f

# Why is this needed?
# If it is removed, the .install files will cause an error
DEB_DH_INSTALL_SOURCEDIR = debian/tmp/


include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk
